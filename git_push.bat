@echo off
git config user.email "dashboard@lme.com"
git config user.name "Dashboard LME"
git commit -m "Dashboard LME completo com graficos interativos e otimizacoes"
git branch -M main
git push -u origin main
pause
